{% extends 'JeanDeJeanPortfolioBundle::layout.html.twig' %}
{% block header_page %}
<div class="page-header">
    <h1>{{ home.getName()|capitalize }}</h1>
    <p class="text-info">{{ home.getHeader() }}</p>
</div>
{% endblock %}
{% block body %}
    <p>{{ home.getContent() }}</p>
{% endblock %}

