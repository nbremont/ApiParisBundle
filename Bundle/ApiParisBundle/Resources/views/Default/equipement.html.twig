{% extends '::base.html.twig' %}
{% block header_page %}
<div class="page-header">
    <h1>Api {Paris}</h1>
    <p class="text-info">Equipement</p>
</div>
{% endblock %}
{% block body %}
    {% for elem in equipement.data %}
    <div class="page-header">
        <p class="text-info">{{ elem.name|raw }}</p>
    </div>
    <p>{{ elem.description|raw }}</p>
    {% endfor %}
{% endblock %}
